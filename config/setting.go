package config

//display name for the cli app
const AppName = "gobackup"

const DisplayName = "GOBACKUP"

const AppShortDesc = "A lightweight automated backup files software."

const AppLongtDesc = "A lightweight automated backup files software."

const AppVersion = "1.0.0"
